name: Pipeline Qualité Eau - CI/CD
on:
  workflow_dispatch: {}

env:
  PYTHON_VERSION: '3.10'
  DATABRICKS_HOST: "https://${{ secrets.DATABRICKS_HOST }}"  # Ajout de "https://"
  DATABRICKS_TOKEN: ${{ secrets.TOKEN_AZUREDATABRICKS }}

jobs:
  # ============================================================
  # JOB 1 : ANALYSE DU CONTENEUR "RAW" (GitHub Actions)
  # ============================================================
  check-raw-files:
    name: "🔍 Étape 1 - Analyse du conteneur 'raw'"
    runs-on: ubuntu-latest
    outputs:
      has_zip: ${{ steps.check-raw.outputs.has_zip }}

    steps:
      - name: 📦 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Configuration Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: 📚 Installation des dépendances Azure
        run: |
          pip install azure-storage-blob

      - name: 🔐 Configuration des variables d'environnement
        run: |
          echo "AZURE_STORAGE_ACCOUNT_NAME=${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}" >> $GITHUB_ENV
          echo "AZURE_STORAGE_ACCOUNT_KEY=${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}" >> $GITHUB_ENV
          echo "CONTAINER_RAW=${{ secrets.CONTAINER_RAW }}" >> $GITHUB_ENV

      - name: 🔍 Vérification des fichiers dans le conteneur "raw"
        id: check-raw
        run: |
          echo "================================"
          echo "🔍 ANALYSE DU CONTENEUR 'RAW'"
          echo "================================"

          python3 << 'EOF'
          from azure.storage.blob import BlobServiceClient
          import os

          # Récupérer les variables d'environnement
          storage_account_name = os.environ.get("AZURE_STORAGE_ACCOUNT_NAME")
          storage_account_key = os.environ.get("AZURE_STORAGE_ACCOUNT_KEY")
          container_name = os.environ.get("CONTAINER_RAW")

          if not all([storage_account_name, storage_account_key, container_name]):
              raise ValueError("❌ Les variables d'environnement ne sont pas configurées")

          # Connexion au compte de stockage
          connection_string = f"DefaultEndpointsProtocol=https;AccountName={storage_account_name};AccountKey={storage_account_key};EndpointSuffix=core.windows.net"
          blob_service_client = BlobServiceClient.from_connection_string(connection_string)

          # Lister les fichiers dans le conteneur "raw"
          container_client = blob_service_client.get_container_client(container_name)
          blobs = list(container_client.list_blobs())

          if not blobs:
              print("❌ Aucun fichier trouvé dans le conteneur 'raw'")
              print("::set-output name=has_zip::false")
          else:
              has_zip = any(blob.name.endswith('.zip') for blob in blobs)
              if has_zip:
                  print("✅ Fichiers .zip trouvés dans 'raw' → Passage direct à l'étape 3")
                  print("::set-output name=has_zip::true")
              else:
                  print("ℹ️  Fichiers trouvés, mais pas de .zip → Exécution de l'ingestion locale")
                  print("::set-output name=has_zip::false")
          EOF

  # ============================================================
  # JOB 2 : INGESTION LOCALE (GitHub Actions)
  # ============================================================
  ingestion-local:
    name: "📥 Étape 2 - Ingestion Locale"
    needs: check-raw-files
    if: needs.check-raw-files.outputs.has_zip == 'false'
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout du code
        uses: actions/checkout@v4

      - name: 🐍 Configuration Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: 📚 Installation des dépendances
        run: |
          echo "================================"
          echo "📚 INSTALLATION DES DÉPENDANCES"
          echo "================================"

          python -m pip install --upgrade pip

          if [ -f requirements.txt ]; then
            echo "📄 Installation depuis requirements.txt"
            pip install -r requirements.txt
          fi

          echo "☁️  Installation des packages Azure"
          pip install azure-storage-blob azure-identity azure-core

          echo "📓 Installation de papermill"
          pip install papermill nbformat nbconvert jupyter

          pip install python-dotenv

          echo "✅ Toutes les dépendances sont installées"
          pip list | grep azure

      - name: 🔐 Configuration des variables d'environnement
        run: |
          echo "================================"
          echo "🔐 CONFIGURATION DES SECRETS"
          echo "================================"

          echo "AZURE_STORAGE_ACCOUNT_NAME=${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}" >> $GITHUB_ENV
          echo "AZURE_STORAGE_ACCOUNT_KEY=${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}" >> $GITHUB_ENV
          echo "AZURE_STORAGE_CONNECTION_STRING=${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" >> $GITHUB_ENV
          echo "CONTAINER_RAW=${{ secrets.CONTAINER_RAW }}" >> $GITHUB_ENV
          echo "CONTAINER_BRONZE=${{ secrets.CONTAINER_BRONZE }}" >> $GITHUB_ENV

          echo "✅ Variables d'environnement configurées"

      - name: 📝 Vérification du notebook
        run: |
          echo "================================"
          echo "📝 VÉRIFICATION DU NOTEBOOK"
          echo "================================"

          if [ ! -f "00_qualite_eau_ingestion.ipynb" ]; then
            echo "❌ Le fichier 00_qualite_eau_ingestion.ipynb n'existe pas"
            exit 1
          fi

          echo "✅ Notebook trouvé : 00_qualite_eau_ingestion.ipynb"

          python3 << EOF
          import json
          with open('00_qualite_eau_ingestion.ipynb', 'r', encoding='utf-8') as f:
              nb = json.load(f)
              print(f"📊 Nombre de cellules : {len(nb['cells'])}")
              print(f"📊 Kernel : {nb['metadata'].get('kernelspec', {}).get('name', 'N/A')}")
          EOF

      - name: 🚀 Exécution du notebook d'ingestion
        run: |
          echo "================================"
          echo "📥 ÉTAPE 2 : INGESTION DES DONNÉES"
          echo "================================"

          export AZURE_STORAGE_ACCOUNT_NAME="${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}"
          export AZURE_STORAGE_ACCOUNT_KEY="${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}"
          export AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}"
          export CONTAINER_RAW="${{ secrets.CONTAINER_RAW }}"
          export CONTAINER_BRONZE="${{ secrets.CONTAINER_BRONZE }}"

          papermill \
            00_qualite_eau_ingestion.ipynb \
            00_qualite_eau_ingestion_output.ipynb \
            --log-output \
            --progress-bar \
            --request-save-on-cell-execute

          echo ""
          echo "✅ Ingestion terminée avec succès"

      - name: 📊 Vérification des résultats
        if: success()
        run: |
          echo "================================"
          echo "📊 VÉRIFICATION DES RÉSULTATS"
          echo "================================"

          echo "✅ Notebook exécuté : 00_qualite_eau_ingestion.ipynb"
          echo "📄 Output généré : 00_qualite_eau_ingestion_output.ipynb"

          if [ -f "00_qualite_eau_ingestion_output.ipynb" ]; then
            echo "✅ Fichier output créé avec succès"

            python3 << EOF
            import json
            with open('00_qualite_eau_ingestion_output.ipynb', 'r', encoding='utf-8') as f:
                nb = json.load(f)
                cells_executed = sum(1 for cell in nb['cells'] if cell.get('execution_count'))
                print(f"📊 Cellules exécutées : {cells_executed}/{len(nb['cells'])}")
            EOF
          else
            echo "⚠️  Fichier output non trouvé"
          fi

      - name: 💾 Upload du notebook exécuté (artifact)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: notebook-ingestion-output
          path: |
            00_qualite_eau_ingestion_output.ipynb
            *.log
          retention-days: 7

      - name: ❌ Gestion des erreurs
        if: failure()
        run: |
          echo "================================"
          echo "❌ ERREUR LORS DE L'INGESTION"
          echo "================================"

          echo "📄 Consultez les logs ci-dessus pour plus de détails"
          echo "💡 Vérifiez :"
          echo "   - Les credentials Azure sont corrects"
          echo "   - Le container existe"
          echo "   - Les permissions sont configurées"
          exit 1

      - name: ✅ Résumé de l'exécution
        if: success()
        run: |
          echo "================================"
          echo "✅ INGESTION TERMINÉE AVEC SUCCÈS"
          echo "================================"

          echo "📊 Résumé :"
          echo "   ✅ Notebook exécuté : 00_qualite_eau_ingestion.ipynb"
          echo "   ✅ Output disponible : 00_qualite_eau_ingestion_output.ipynb"
          echo "   ✅ Artifacts uploadés : notebook-ingestion-output"
          echo ""
          echo "🎉 Étape 2 du pipeline complétée !"

  # ============================================================
  # JOB 3 : EXÉCUTION DU PIPELINE "ETL v1" SUR DATABRICKS
  # ============================================================
  run-databricks-pipeline:
    name: "🚀 Étape 3 - Exécution du pipeline 'ETL v1' sur Databricks"
    needs: check-raw-files
    if: needs.check-raw-files.outputs.has_zip == 'true' || (needs.check-raw-files.outputs.has_zip == 'false' && needs.ingestion-local.result == 'success')
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout du code
        uses: actions/checkout@v4

      - name: 🚀 Lancement du pipeline "ETL v1" sur Databricks
        run: |
          echo "================================"
          echo "🚀 LANCEMENT DU PIPELINE 'ETL v1'"
          echo "================================"

          echo "DATABRICKS_HOST: ${{ env.DATABRICKS_HOST }}"
          echo "DATABRICKS_TOKEN: ${TOKEN_AZUREDATABRICKS:+defined}"

          # Remplace 836231694088498 par l'ID de ton job "ETL v1"
          response=$(curl -X POST \
            -H "Authorization: Bearer ${{ env.DATABRICKS_TOKEN }}" \
            -H "Content-Type: application/json" \
            "${{ env.DATABRICKS_HOST }}/api/2.1/jobs/run_now" \
            -d '{
              "job_id": 836231694088498,
              "notebook_params": {
                "notebook_path": "/raw_to_bronze_result_and_plv.ipynb"
              }
            }')

          echo "📋 Réponse de l'API Databricks :"
          echo "$response"

          # Vérifier si le lancement a réussi
          run_id=$(echo "$response" | jq -r '.run_id')
          if [ "$run_id" != "null" ]; then
            echo "✅ Pipeline 'ETL v1' lancé avec succès (Run ID: $run_id)"
          else
            echo "❌ Échec du lancement du pipeline"
            exit 1
          fi

      - name: 🔄 Vérification du statut du pipeline
        run: |
          echo "================================"
          echo "🔄 VÉRIFICATION DU STATUT DU PIPELINE"
          echo "================================"

          # Remplace $run_id par l'ID réel de ton run (à récupérer depuis la réponse précédente)
          sleep 30
          status=$(curl -X GET \
            -H "Authorization: Bearer ${{ env.DATABRICKS_TOKEN }}" \
            "${{ env.DATABRICKS_HOST }}/api/2.1/jobs/runs/get?run_id=$run_id" | jq -r '.state.life_cycle_state')

          echo "📋 Statut du pipeline : $status"

          if [ "$status" == "TERMINATED" ]; then
            echo "✅ Pipeline 'ETL v1' terminé avec succès"
          elif [ "$status" == "RUNNING" ]; then
            echo "ℹ️  Pipeline en cours d'exécution"
          else
            echo "❌ Pipeline en échec ou statut inconnu"
            exit 1
          fi

      - name: ❌ Gestion des erreurs
        if: failure()
        run: |
          echo "================================"
          echo "❌ ERREUR LORS DE L'EXÉCUTION DU PIPELINE"
          echo "================================"

          echo "📄 Consultez les logs ci-dessus pour plus de détails"
          echo "💡 Vérifiez :"
          echo "   - L'ID du job est correct"
          echo "   - Le token Databricks est valide"
          echo "   - Le cluster est disponible"
          exit 1

      - name: ✅ Résumé de l'exécution
        if: success()
        run: |
          echo "================================"
          echo "✅ PIPELINE 'ETL v1' TERMINÉ AVEC SUCCÈS"
          echo "================================"

          echo "📊 Résumé :"
          echo "   ✅ Pipeline 'ETL v1' lancé sur Databricks"
          echo "   ✅ Statut vérifié avec succès"
          echo ""
          echo "🎉 Étape 3 du pipeline complétée !"
